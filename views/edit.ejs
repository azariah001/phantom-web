<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="phantom">
      <img src="/images/phantom.gif" alt="Minecraft phantom GIF">
      <% if (currentVersion === newVersion) { %>
        <div class="version">Version: <%= currentVersion %></div>
      <% } else { %>
        <div class="version">Updating!</div>
      <% } %>
    </div>

    <div class="container">
      <div class="menu">
        <div class="title">
          <div class="text">
            <%= title %>
          </div>
          <a class="close" href="/">
            <img src="/images/menu-close.png" alt="Minecraft close dialogue">
          </a>
        </div>
        <div class="content">
          <form method="post" action="/server/edit">
            <div class="border">
              <label for="server[name]">Server Name</label>
              <input type="text" name="server[name]" value="<%= server.name %>">
              <label for="server[address]">Server Address</label>
              <input type="text" name="server[address]" value="<%= server.address %>">
              <label for="server[port]">Port</label>
              <input type="text" name="server[port]" value="<%= server.port %>">
              <input type="text" name="server[index]" value="<%= server.index %>" style="display:none">
              <label for="server[auto]">Auto</label>
              <input class="checkbox" type="checkbox" name="server[auto]"<% if (server.auto) { %> checked<% } %>>
            </div>
            <div class="group">
              <a class="button" href="/server/delete/<%= server.index %>"><p>Delete</p></a>
              <input class="button" type="submit" value="Save">
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
